# Fail installs loudly on toolchains older than package.json "engines".
# npm < 10.9 has an optional-dependencies bug (npm/cli#4828) that silently
# skips vite's native rolldown binding and crashes at dev time with a cryptic
# error. This converts that into a clear EBADENGINE message at install time.
engine-strict=true
